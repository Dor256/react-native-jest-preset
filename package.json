{
  "name": "@unpacked/react-native-jest-preset",
  "version": "1.0.2",
  "description": "A custom React Native Jest preset",
  "main": "dist/jest-preset.js",
  "files": [
    "*.js"
  ],
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Dor256/react-native-jest-preset.git"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && tsc",
    "prepublish": "yarn build"
  },
  "author": "dorb256@gmail.com",
  "license": "ISC",
  "peerDependencies": {
    "react-native": "*"
  },
  "devDependencies": {
    "@types/node": "^16.4.13"
  }
}
